.media#new-comment
  a.pull-left
    = image_tag current_user.avatar_url, size: '40x40', class: 'img-rounded'
  .media-body
    = form_for [@node, @comment] do |f|
      .form-group
        = f.text_area :body, class: 'form-control'
      .row
        .col-lg-6
          p You can use markdown to style your comment.
        .col-lg-6
          = f.submit 'Add comment', class: 'btn btn-xs pull-right'